---
title: "R para Ingenierías"
author: "La Academia"
subtitle: "Tema 05 - Práctica"
output:
  html_document:
    df_print: paged
---

# Ingreso de datos por el usuario y visualización personalizada

## Ejercicio 1

Desarrolla un programa que solicite al usuario la velocidad inicial (en m/s), la aceleración (en m/s²), y el tiempo (en s) de un objeto en movimiento. Calcula la velocidad final utilizando la fórmula $v_f = v_i + at$ y la distancia recorrida utilizando $d = v_i t + \frac{1}{2}at^2$. Imprime estos resultados de manera estructurada.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------

## Ejercicio 2

Escribe un programa que acepte un entero $n$ y compute el valor de $n + nn + nnn$.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------

## Ejercicio 3

Escribe un programa que solicite al usuario los valores de resistencia $R$ (en ohmios), inductancia $L$ (en henrios), y capacitancia $C$ (en faradios) de un circuito RLC en serie. Calcula la frecuencia resonante del circuito usando la fórmula $f_r = \frac{1}{2\pi\sqrt{LC}}$, y el factor de calidad usando $Q = \frac{1}{R} \sqrt{\frac{L}{C}}$.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------

## Ejercicio 4

Escribe un programa que solicite al usuario la carga axial (en kN) y la sección transversal (en cm²) de una columna. Calcula la tensión axial usando la fórmula $\sigma = P / A$, donde $P$ es la carga axial y $A$ es la sección transversal. Imprime los resultados en un formato claro y legible. La tensión axial $\sigma$ debe estar expresada en Pascales.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------

## Ejercicio 5

Solicita al usuario el coeficiente de rugosidad $n$, el área transversal $A$ (m²), el radio hidráulico $R$ (m) y la pendiente $S$. Calcula el caudal utilizando la ecuación de Manning:

$$
Q = \frac{1}{n} A R ^ {2/3} S ^ {1/2}
$$

Imprime los resultados en un formato claro y legible.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------

## Ejercicio 6

Pide al usuario la longitud de la columna $L$ (m), el momento de inercia $I$ (m⁴) y el módulo de elasticidad $E$ (Pa). Calcula la carga crítica de pandeo usando la ecuación de Euler:

$$
P_{cr} = \frac{\pi^2EI}{L^2}
$$

Imprime los resultados en un formato claro y legible.

> [**Nota:**]{.underline} Recuerda utilizar el **enfoque EPS**.

## Solución

```{r}
# Ingresa tu código aquí 👽
```

------------------------------------------------------------------------
